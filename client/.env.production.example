# Production build - copy to .env.production before running npm run build
# These are baked into the client bundle at build time.

# API base URL (your backend; e.g. same domain or subdomain)
REACT_APP_API_URL=https://connect.refined-digital.co.za/api

# Public URL of the app (for asset paths and router base)
# If the app is in a subfolder use e.g. https://connect.refined-digital.co.za/crm
PUBLIC_URL=https://connect.refined-digital.co.za
