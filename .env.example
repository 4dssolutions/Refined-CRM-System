# =============================================================================
# Refined CRM - Environment variables (copy to .env and fill in)
# Server reads .env from project root. Never commit .env to version control.
# =============================================================================

# --- Server ---
NODE_ENV=production
PORT=5000

# App URL (for password reset emails etc.)
APP_URL=https://connect.refined-digital.co.za

# JWT secret for session tokens (use a long random string in production)
JWT_SECRET=your-secret-key-change-in-production

# --- Database ---
# Use SQLite (no MySQL needed): set DB_USE_SQLITE=true or leave MySQL unconfigured.
# SQLite file path when using SQLite (default: ./data/refined_crm.sqlite).
# DB_USE_SQLITE=true
# DB_PATH=./data/refined_crm.sqlite

# MySQL (optional; if not set or connection fails, app falls back to SQLite):
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=refined_crm
# DB_USER=root
# DB_PASSWORD=your_password

# --- SMTP (optional, for real outbound email) ---
# Required for sending: SMTP_HOST, SMTP_USER, SMTP_PASS
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM_EMAIL=your-email@gmail.com
SMTP_FROM_NAME=Refined CRM

# --- Client build (used when running `npm run build` in client) ---
# API base URL the browser will call (your Node API on cPanel or same domain)
REACT_APP_API_URL=https://connect.refined-digital.co.za/api

# Public URL of the app (for asset paths and React Router base). Use subfolder path if needed, e.g. https://connect.refined-digital.co.za/crm
PUBLIC_URL=https://connect.refined-digital.co.za
